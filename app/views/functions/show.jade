extends ../layouts/default

block content

  h2: strong: a(href="/functions")= title
  table(class="table table-hover")
      thead
        tr
          th ID
          th Nombre
          th Path
      tbody
          tr
            td #{functions._id}
            td #{functions.name}
            td #{functions.path}

  form(method="post", action="functionss/update")
    fieldset
      legend Actualizar #{title}
      input(type="hidden", value="#{functions._id}", name="id")
      div.form-group
        label Nombre
        input(type="text", class="form-control", placeholder="Nombre", name="name", value="#{functions.name}")
      div.form-group
        label Path
        input(type="text", class="form-control", placeholder="Path", name="path", value="#{functions.path}")

      button(type="submit", class="btn btn-default") Actualizar