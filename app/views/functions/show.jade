extends ../layouts/default

block content

  h2: strong: a(href="/functions")= title
  table(class="table table-hover")
      thead
        tr
          th ID
          th Nombre
          th Path
      tbody
          tr
            td #{fnctions._id}
            td #{fnctions.name}
            td #{fnctions.path}

  form(method="post", action="functions/update")
    fieldset
      legend Actualizar #{title}
      input(type="hidden", value="#{fnctions._id}", name="id")
      div.form-group
        label Nombre
        input(type="text", class="form-control", placeholder="Nombre", name="name", value="#{fnctions.name}")
      div.form-group
        label Path
        input(type="text", class="form-control", placeholder="Path", name="path", value="#{fnctions.path}")

      button(type="submit", class="btn btn-default") Actualizar